<%= form_with(model: company_application, local: true) do |form| %>
  <% if company_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_application.errors.count, "error") %> prohibited this company_application from being saved:</h2>

      <ul>
      <% company_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
