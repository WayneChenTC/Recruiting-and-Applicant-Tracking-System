<ul>
<% @applications.each do |application| %>
	<li>
	  <%= application.name %>
	  <%= link_to 'show', user_application_path(@user, application) %>
	  <%= link_to 'edit', edit_user_application_path(@user, application) %>
	  <%= link_to 'destroy', user_application_path(@user, application),
	                            :method => :delete %>
	</li>
<% end %>

</ul>

	  <%= link_to 'new application', new_user_application_path(@user) %>
