<h1>Editing Job</h1>

<%= form_with(model: [@company, @job], url: company_job_path, local: true) do |form| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="title">
    <h1><%= Company.find(params[:company_id]).name %>

  </div>

  <div class="field">
    <%= form.label :job_description %>
    <%= form.text_field :job_description, id: :job_description %>
  </div>

  <div class="field">
    <%= form.label :employment_type %>
    <%= form.text_field :employment_type, id: :employment_type %>
  </div>

  <div class="field">
    <%= form.label :responsibilities %>
    <%= form.text_field :responsibilities, id: :responsibilities %>
  </div>

  <div class="field">
    <%= form.label :requirements %>
    <%= form.text_field :requirements, id: :job_requirements %>
  </div>

  <div class="actions">
    <%= form.submit "Update" %>
  </div>
<% end %>

<%= link_to 'Show', company_job_path(@job.company, @job) %> |
<%= link_to 'Back', company_jobs_path %>
